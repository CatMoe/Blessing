package catmoe.fallencrystal.moefilter.common.check.joinping

enum class JoinPingType {
    DISABLED,
    STABLE,
    ONLY_JOIN,
    ONLY_PING,
    PING_FIRST,
    JOIN_FIRST,
    PING_FIRST_REST,
    JOIN_FIRST_REST,
}